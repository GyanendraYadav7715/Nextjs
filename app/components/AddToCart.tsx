"use client";
import React from "react";

const AddToCart = () => {
  return (
    <button
      className="btn btn-primary"
      onClick={() => {
        alert("Helo Ia ma ");
      }}
    >
      Add to cart
    </button>
  );
};

export default AddToCart;
